package lambdaexpression;

/*
 * This is a functional interface that has only one function.
 * Ideally a class needs to be defined that implements this interface & 
 * defines the method.
 */
public interface Greetings {
	public void greet(String name);
}
